export interface ProductPromoProps {
  productName: string;
  price: string;
  description: string;
  imageUrl: string;
  hook: string;
  cta: string;
  primaryColor?: string;
  secondaryColor?: string;
}